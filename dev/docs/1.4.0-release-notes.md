# MarkView 1.4.0 Release Notes (Draft)

Status: Unpublished release candidate. Do not publish these notes until the
Windows Store, macOS DMG, and both Snap architectures pass their release gates.

## User-Facing Summary

MarkView 1.4.0 adds offline math typesetting. Inline and display equations in
Markdown are rendered with the bundled KaTeX runtime, with no CDN or network
dependency.

Highlights:

- Inline `$...$` and display `$$...$$` expressions.
- Accessible HTML and MathML output.
- Local, content-addressed JavaScript, CSS, and font assets.
- Readable TeX fallback when an expression or runtime asset cannot be rendered.
- Rendering limits that prevent unusually large documents or expressions from
  monopolizing the page.
- Existing code blocks, escaped currency, links, themes, syntax highlighting,
  and fragment navigation remain ordinary viewer content.

KaTeX typesetting is applied only to math nodes emitted by PowerShell's
Markdown converter. MarkView does not scan arbitrary text or use KaTeX
auto-render.

## Known Limitation

PowerShell's Markdown converter can interpret unescaped dollar-delimited
currency as math. Escape literal dollar signs, for example `\$12.50`. The
upstream issue is
[PowerShell/PowerShell#27792](https://github.com/PowerShell/PowerShell/issues/27792).

## Platform Availability

### Windows

Microsoft Store, x64:
[Get MarkView](https://apps.microsoft.com/detail/9MSWK3Q0JZ5N?hl=en-us&gl=US&ocid=pdpshare)

The Store submission is an unsigned `1.4.0.0` package; Microsoft signs the
published package.

### macOS

Apple Silicon:
[MarkView_1.4.0_arm64.dmg](https://github.com/omasoud/MarkdownViewer/releases/download/v1.4.0/MarkView_1.4.0_arm64.dmg)

Developer ID signed, notarized, stapled, and Gatekeeper-validated.

SHA-256:
`a01922afa0fe5f1de4a6aed2f1dfca3da2556171bce8f98e5d3df9c449e2ef49`

### Linux

Snap Store, amd64 and arm64:
[Install MarkView](https://snapcraft.io/markdownviewer)

`sudo snap install markdownviewer`

Version 1.4.0 is available in `latest/stable` as amd64 revision 4 and arm64
revision 3.

## Microsoft Store Release Notes

- Added offline typesetting for inline and display math with bundled KaTeX.
- Added accessible MathML output and readable fallback for malformed or
  unsupported expressions.
- Preserved themes, syntax highlighting, local Markdown navigation, and strict
  no-network content security behavior.
- Updated MarkView to 1.4.0 with bundled PowerShell 7.6.3.

## GitHub Release Routing Body

MarkView 1.4.0 adds offline KaTeX math typesetting for inline and display
expressions, accessible MathML output, and readable fallback behavior.

### Windows

Install the x64 release from the
[Microsoft Store](https://apps.microsoft.com/detail/9MSWK3Q0JZ5N?hl=en-us&gl=US&ocid=pdpshare).

### macOS

Download the signed and notarized Apple Silicon DMG:
[MarkView_1.4.0_arm64.dmg](https://github.com/omasoud/MarkdownViewer/releases/download/v1.4.0/MarkView_1.4.0_arm64.dmg)

SHA-256:
`a01922afa0fe5f1de4a6aed2f1dfca3da2556171bce8f98e5d3df9c449e2ef49`

### Linux

Install the amd64 or arm64 Snap from the
[Snap Store](https://snapcraft.io/markdownviewer):

`sudo snap install markdownviewer`

### Architecture Scope

- Windows: x64
- macOS: Apple Silicon arm64
- Linux Snap: amd64 and arm64

### Known Limitation

Escape literal currency dollar signs in Markdown, such as `\$12.50`, because
the current PowerShell Markdown converter can otherwise interpret
dollar-delimited text as math.
